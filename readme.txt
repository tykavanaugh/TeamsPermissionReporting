README.txt

Open the xml file in task scheduler and change the path in actions and update the trigger to desired interval. Move powershell script to path. Will require login during the first time it executes.